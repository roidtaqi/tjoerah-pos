import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1E1E1E); // Dark charcoal/almost black
  static const Color primaryDark = Color(0xFF000000);
  static const Color accent = Color(0xFF007AFF); // iOS style blue for actions
  
  static const Color background = Color(0xFFF8F9FA); // Off-white/light gray
  static const Color surface = Colors.white;
  static const Color surfaceDark = Color(0xFF2C2C2E);

  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF8E8E93);
  static const Color textInverse = Colors.white;

  static const Color border = Color(0xFFE5E5EA);
  static const Color divider = Color(0xFFE5E5EA);

  // Status colors
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF9500);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF5AC8FA);
}
